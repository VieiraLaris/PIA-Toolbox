function setupPIA()

root = fileparts(mfilename('fullpath'));

addpath(fullfile(root, 'core'));
addpath(fullfile(root, 'engine'));
addpath(fullfile(root, 'functions'));
addpath(fullfile(root, 'tests'));

end
